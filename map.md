# map.md — 共同作業マップ

Claude Code と Codex(ChatGPT) が共有する作業台帳。**セッション開始時に必読、終了時に追記する。**
運用ルールは [CLAUDE.md](CLAUDE.md) / [AGENTS.md](AGENTS.md) を参照。

---

## 1. 現在地

- プロジェクト: G-survival -隙間の生- 第1章(クロゴキブリ1齢幼虫の脱皮まで)
- 技術: three.js + TypeScript + Vite / 横スクロール2.5D
- 状態: **第1章モック完成。改善フェーズ**
- ブランチ: `main` 直コミット運用(§5)
- 最新コミット時点の記録: `IMPLEMENTATION_STATUS.md`(修正・検証結果) / `SPEC_GAP_AUDIT.md`(2026-09-06時点の差分監査、**現在の未実装一覧ではない**)
- 一次仕様: `gokiburi_ch1_spec.md`(変更しない。仕様変更は本ファイル §4 に記録する)

---

## 2. 担当分界(オーナーシップ)

同一ファイルの同時編集を避けるための暫定分界。**着手時に「作業中」を立て、終了時に外す。**

| 領域 | ファイル | 現オーナー | 状態 |
|---|---|---|---|
| ゲームループ・描画・HUD | `src/main.ts` | — | 空き |
| シミュレーション(移動/敵/資源/群れ) | `src/simulation.ts` | — | 空き |
| 生物の3D造形・アニメ | `src/animals.ts` | — | 空き |
| 脱皮演出 | `src/molt.ts` | — | 空き |
| 音響 | `src/audio.ts` | — | 空き |
| 検証スクリプト | `*.mjs`, `scripts/` | — | 空き |
| ドキュメント | `*.md` | 共有(追記のみ) | — |

> `src/main.ts` は1行1492文字の高密度コード。**同時編集は必ず衝突する。** オーナーを取らずに触らない。

---

## 3. 申し送り・質問(INBOX)

相手エージェント宛のメッセージ。`OPEN` は未処理、`ANSWERED` は回答済み、`CLOSED` は完了。
新規は一番上に追加する。

| # | 日付 | From→To | 状態 | 内容 |
|---|---|---|---|---|
| 004 | 2026-09-08 | Claude→Codex | OPEN | `docs/isometric-refactor-brief.md`(斜俯瞰3Dリファクタ)に着手しました。フェーズ1(カメラのみ)は完了。**フェーズ2以降は `src/simulation.ts` の座標系(2D `{x,y}` → 面ベース)と `OBSTACLES` の作り直しを伴う**ため、着手前に §2 でオーナーを取ってください。指示書のとおり `src/animals.ts` は Blender 差し替え作業中につき編集禁止です。 |
| 001 | 2026-09-08 | Claude→Codex | OPEN | map.md / CLAUDE.md / AGENTS.md による共同運用を開始しました。Codex側は次回セッション開始時に AGENTS.md を読み、終了時に §6 セッションログへ追記してください。分界(§2)のオーナー取得もお願いします。 |
| 003 | 2026-09-08 | Claude→ガクチョ | CLOSED | `git push origin main` が Claude 側の権限設定で拒否された件。→ **ガクチョが `.claude/settings.json` の deny から `Bash(git push:*)` を削除し、push 完了(`cd49511..366f11d`)。** force push は CLAUDE.md / AGENTS.md §4 の規約で禁止する運用とする。 |
| 002 | 2026-09-08 | Claude→ガクチョ | ANSWERED | 改善の優先順位。→ **回答: まず実プレイ検証(E12)。テストモードを立ち上げ、ガクチョがプレイしてから指示を出す。** |

---

## 4. 改善バックログ

`SPEC_GAP_AUDIT.md`(2026-09-06)の指摘のうち、`IMPLEMENTATION_STATUS.md`(2026-09-07)で解消されたものを除いた残件。
**状態の根拠が「コード確認済み」でないものは推定として扱う。**

### P1 — 体験の芯に関わる

| ID | 内容 | 根拠 | 状態 |
|---|---|---|---|
| C01 | マウス方向へ触角モデルが向く。現状は探知扇形だけが向き、触角は歩行に連動して揺れるだけ | 原仕様 §4/§5.1 | 未着手(`grep antenna` でヒットなし = 未実装と判断) |
| C06 | 気流レーダーを全方位化。現状は最寄りのクモ1匹について左右端のみ | 原仕様 §5.2 | 未着手(コード上 glow は main.ts のみ) |
| C07 | すべての致命イベントに0.4〜1.2秒の予告を保証(アリ・人間・複数同時) | 原仕様 §9-1 フェアネス規約 | 未着手 |
| C17 | 死の演出。**文言はガクチョ指定で「Gが{n}体、減りました」に変更済み(2026-09-08)。** 残るのは視覚面 — 白帯が消えて闇に溶ける動きがなく、現状は即座に非表示 | 原仕様 §8 | 部分対応(通知はある / fade 演出なし) |
| R01 | 斜俯瞰3Dリファクタ(`docs/isometric-refactor-brief.md`)。フェーズ1=カメラ / 2=世界をX-Z床面に再構築 / 3=壁登り / 4=暗闇 / 5=敵AIの3D経路 | ガクチョ指示書 | **フェーズ1〜4完了(2026-09-08)**。**フェーズ5(敵の壁登り)はガクチョ判断で不要**。指示書は完了 |
| E16 | 正方形化+壁登り後の難易度をガクチョが実プレイで再判定 | E12 の再実施 | **完了(2026-09-08)**: 「ゲームバランスもちょうどいい」。FPS も終始60で安定。指摘は壁上の左右反転1件のみで、修正済み |
| E12 | 初見プレイで5〜8分に収まるかの実測と調整 | 原仕様 §11 | **一部検証(2026-09-08)**: ガクチョが実プレイし「設計意図通りの難易度」と判定。難易度は現状維持で確定。**所要時間の実測値は未取得**のため、5〜8分の条件は引き続き未検証 |

### P2 — 密度・手触り

| ID | 内容 | 根拠 | 状態 |
|---|---|---|---|
| C02 | 探知距離の数値定義。原仕様の 40px/150px/250px を2.5D向けに再定義して明文化 | 原仕様 §5.1 | 未整理 |
| C03 | 探知した物体の輪郭表示。現状は明度マスク+実体表示 | 原仕様 §5.1 | 部分実装 |
| C12 | 空腹で視界半径と触角感度が落ちる(速度だけでなく) | 原仕様 §5.4 | 部分実装(`sense()` はあるが視界半径への反映を要確認) |
| E14 | 家鳴り、素材に応じた6本足の接地音 | 原仕様 §8 | 未実装(`creak`/`footstep` なし) |
| M08 | 脱皮中にBGMを抑え、心音と危険音を聞き取れるようにする | 原仕様 §8 | 部分実装(心音あり、BGM抑制は未確認) |
| E15 | 気流音の上下区別・危険種別・複数同時の混合 | 原仕様 §5.2 | 部分実装 |

### P3 — 検証・整理

| ID | 内容 | 状態 |
|---|---|---|
| V01 | `SPEC_GAP_AUDIT.md` を現行コードで再監査し、解消済み項目を明示する | 未着手 |
| V02 | 初見プレイの記録(所要時間 / 死因 / 群れ減少曲線 / 脱皮場所)を残す枠組み | 未着手 |
| V03 | `src/main.ts` の可読性。1行1492文字は共同編集の事故要因 | 要判断(**ガクチョの承認なしに大規模整形はしない**) |

### 仕様変更ログ(原仕様からの意図的な逸脱)

| 日付 | 項目 | 変更内容 | 決定者 |
|---|---|---|---|
| 〜2026-09-07 | 技術 | Canvas 2D 単一ファイル → three.js + TypeScript 複数ファイル | ガクチョ |
| 〜2026-09-07 | 視点 | 平面8方向 → 横スクロール2.5Dの壁面移動 | ガクチョ |
| 〜2026-09-07 | UI | 説明なし → タイトル・導入文・目的・残数HUD | ガクチョ |
| 〜2026-09-07 | 音 | BGMなし → BGMあり + 音量/ミュート | ガクチョ |
| 〜2026-09-07 | 群れ | 24匹・弱く追従・最寄りへ交代 → 12匹・独立散開・疑似乱数で交代 | 12匹はAI暫定値/他はガクチョ |
| 〜2026-09-07 | 敵表示 | 白い線画の輪郭のみ → 実体のある3Dモデル | ガクチョ |
| 2026-09-08 | 喪失の表現 | 「白い帯がひとつ、消えた。」→「Gが{n}体、減りました」。全滅時「白い帯は、もう動かない。」→「Gの群れは、全滅した。」 | ガクチョ |
| 2026-09-08 | 目標表示 | HUD に「脱皮に必要な蓄え」を追加。餌・水を5段階の●○で表示し、満ちたら「準備ができました — 安全な場所で Space を長押し（3秒）」と明示 | ガクチョ |
| 2026-09-08 | 視点 | 正面平行投影(OrthographicCamera)→ **透視投影の斜俯瞰(PerspectiveCamera)**。`docs/isometric-refactor-brief.md` のフェーズ1。世界の座標系(X-Y平面)はまだ変えていない | ガクチョ(指示書) |
| 2026-09-08 | 世界の座標系 | 遊技面を「X-Y に建てた壁の断面」→「**X-Z の床面**」に組み直し。`src/space.ts` を新設して sim(x,y)→world(x,高さ,-y) の対応を1箇所に集約。`simulation.ts` は未変更(2D パラメータ空間として温存)。屋根裏の天井は俯瞰を塞ぐため描いていない | ガクチョ(指示書) |
| 2026-09-08 | 世界の形 | 100×7 の一本道 → **33×33 の正方形の屋根裏**。仕切り壁3枚の切れ目を互い違いに置いて蛇行させ、総経路長を旧118に対し125.5(+6%)に収めた。障害物 7→15。**難易度に影響する変更**(ガクチョが承知の上で指示) | ガクチョ |
| 2026-09-08 | 壁登り | 面を0.35秒押し込むと貼り付いて登る。塊の天面に乗れる。アリは高さ0.3、クモは1.35までしか届かない(仕切り壁の上 1.5 は安全地帯) | ガクチョ(指示書フェーズ3) |
| 2026-09-08 | 明るさ | 覆いの不透明度 .985→.88、周囲の可視半径 1→1.25倍、扇形の抜き .9/.65→.97/.8、露出 1.25→1.4、環境光 2→2.4 | ガクチョ |

---

## 5. Git運用

- 両者とも `main` に直コミット。**作業開始前に必ず `git pull --rebase origin main`。**
- コミットメッセージ冒頭に担当を付ける: `[claude] ...` / `[codex] ...`
- 1コミット1論点。src/ の広範囲書き換えは避け、着手前に §2 でオーナーを取る。
- 相手のコミットを revert / force-push しない。

---

## 6. セッションログ

新しいセッションを**一番上**に追加する。テンプレは §7。

### 2026-09-08 / ガクチョ+Claude / 実機判定と、壁上の左右反転バグ修正・暗闇のフェーズ4

- **ガクチョの実機判定(ここが一次情報)**:
  - **FPS は終始 60 で安定。** Intel Iris Xe。3フェーズ通して未計測だった項目がこれで解消。指示書の「フェーズごとにFPSを計測」は達成。
  - **ゲームバランスはちょうどいい。** 33×33 化と壁登り追加後の難易度は、このまま確定とする(E16 完了)。
  - **フェーズ5(敵の壁登り)は不要。** 「壁は敵が登れないままでいい」。→ 指示書のフェーズ5は**やらない**と決定。
  - 指摘: 壁に登ると左右の移動が入力と逆になる。暗闇の扇形を直してほしい。
- **修正1: 壁上の左右反転(バグ)**:
  - 原因は `onFace()` で、接線 t=(-ny,nx) との内積 `along` を x / y に**そのまま**足していたこと。変位は `along * t` でなければならない。
  - 結果として法線が **(-1,0) と (0,+1) の面だけ**左右が逆になっていた = 塊の西面・北面、東と南の外周壁。ガクチョが最初に登るのは塊の西面(東へ歩いて突き当たる)なので、必ず踏む位置にあった。
  - 4面すべての横移動方向を検証する回帰を追加(smoke 11群目)。**修正を戻すとこの回帰が落ちることを実際に確認済み**(西面で入力 +y に対し y が -0.827)。
- **修正2: 暗闇の扇形(指示書フェーズ4、選択肢 (a) スクリーン空間のまま)**:
  - 遮蔽計算に高さを入れた。従来は自分が乗っている塊の footprint で視界が切れ、天面に立つと足元で光が死んでいた。今は自分より低い塊は見下ろせる。
  - 扇形を「今いる面のパラメータ空間」で組み直した。壁に貼り付くと扇形が面の上に乗り、面の縁(壁の上端・床・面の端)で切れる。従来は自分の高さに水平な扇形が浮いていた。
  - 照準を今いる面に落とすようにした。従来はマウスのレイを常に床平面と交差させていたため、高いところにいると扇形が体からずれていた。触角の向きも同じ空間で取る。
  - 面に貼り付いている間は、床の生き物を扇形で走査できない(手が届く範囲 1.15×sense のみ)。高所の安全と引き換えの制約として明示的に入れた。
  - 床の上の挙動は数式上まったく変わっていない(`surfWorld` は水平面では `toWorld(x,y,PLANE_H+0)` に一致)。
- **変更ファイル**: `src/simulation.ts` `src/main.ts` `smoke.mjs` `map.md` `artifacts/fan-*.png`
- **ゲーム本体の変更**: あり
- **検証**: `bash scripts/verify.sh` **全項目 OK**(build / smoke **11群** / playthrough 229.3秒 state=won / browser-audit PASS)。Playwright で天面・壁面・床の3状態を撮影し、扇形が面に乗ることとページ例外0件を確認(`artifacts/fan-a-ontop.png` `fan-b-onwall.png` `fan-c-floor.png`)。
- **未完・引き継ぎ**:
  - 面の端まで来ると止まる(角を回り込んで隣の面へ移らない)。指示書 §2 の「法線を補間して乗り移る」は未実装のまま。ガクチョから指摘が出ていないので保留。
  - 天井は描いていない(俯瞰の妨げになるため)。
  - 落下ダメージ・スタミナ消費・滑落は指示書のとおり後回しのまま。
  - **これで `docs/isometric-refactor-brief.md` の残件はフェーズ5(不要と決定)のみ。指示書は完了とみなす。**
- **オーナー解放**: `src/main.ts` `src/simulation.ts` `smoke.mjs` を本セッション中のみ保持し、終了時に解放。

### 2026-09-08 / Claude / フェーズ3 — 屋根裏を正方形に広げ、壁登りを実装

ガクチョの指示「壁のぼりを可能に。世界を手前にも広げ、横長ではなく屋根裏らしく正方形に近い構造に」。**難易度に影響する変更であることを事前に伝えた上での指示**として実施。

- **世界の形(コミット `c95674b`)**:
  - `WORLD{lo:1,hi:34}` を導入。移動・経路探索の格子・探索済み範囲・外周判定をすべて正方形に。
  - 仕切り壁3枚(y=9 / 17 / 25)の切れ目を互い違いに置いて蛇行させた。**着手前に到達性と経路長を検証スクリプトで確認**し、旧 100×7 の総経路長118に対し125.5(+6%)になるよう寸法を決めてから実装した。障害物 7 → 15。
  - 資源9個・休息点3個・アリ2経路・クモ2匹を、**量と役割を変えずに**再配置。「餌4.0 はクモの巡回線上、水4.0 は約3 unit 離す」という旧構図を踏襲した。
  - 仲間の散開を通路方向 ±15 から全方位 半径4〜11.2 に置き換えた(密集させるとアリ列に全員で突っ込んで全滅する)。
- **壁登り(コミット `dbfe84d`)**:
  - プレイヤーに `z`(高さ)と `(nx,ny)`(貼り付いている面の外向き法線)を持たせた。指示書 §1 のとおり、面の上は「面に沿う水平方向 × 高さ」の2Dパラメータ空間として扱う。
  - 面を **0.35秒押し込む**と貼り付く。予告なく貼り付かないための間(フェアネス §9-1)。面へ押す入力が「登る」、離れる向きが「降りる」。
  - 塊の天面に乗れる。踏み外すとその場で下の面まで降りる(落下ダメージ・スタミナ消費・滑落は指示書のとおり後回し)。
  - 姿勢は接触面の法線から決める(`src/space.ts` の `pose()`)。**カメラは回さない。プレイヤーだけが回る。** `animateAnimal` の `rotation.z` を台座の基底ごと差し替えているので **`src/animals.ts` は今回も未編集**。
  - アリは高さ0.3、クモは1.35までしか届かない。**仕切り壁の上(top 1.5)はクモも届かない安全地帯**になり、「登る技術が安全に直結する」(フェアネス §9-3)。
  - 壁に貼り付いたままは脱皮できない。資源と糞の山は高さ0.35以下でのみ使える。
- **変更ファイル**: `src/simulation.ts` `src/main.ts` `src/space.ts` `src/molt.ts` `smoke.mjs` `playthrough.mjs` `browser-audit.mjs` `map.md` `artifacts/square-*.png` `artifacts/climb-*.png`
- **ゲーム本体の変更**: あり(**シミュレーションを含む全面**)
- **検証**: `bash scripts/verify.sh` **全項目 OK**。
  - `smoke.mjs` **10群**(壁登りの回帰を1群追加: 0.35秒未満では貼り付かない / 西面に貼り付く座標 / 天面に登りきる / 踏み外して降りる / 高所ではアリが取り付かない・床に降りれば従来どおり群がる / 壁では脱皮できず床では脱皮できる)。
  - `playthrough.mjs --probe`: **旧レイアウト 218.1秒 state=won 群れ1 死1 → 新 229.3秒 state=won 群れ2 死0**。旧レイアウトの数値は `git show HEAD:src/simulation.ts` を別ディレクトリで実行して実測した。
  - `browser-audit.mjs` PASS。加えて Playwright で実際にキー入力して壁を登らせ、貼り付き→登坂→天面という遷移を座標で確認し撮影した(`artifacts/climb-a-onwall.png` `climb-c-ontop.png` `climb-d-default-onwall.png`)。
- **未完・引き継ぎ**:
  - **難易度はガクチョの再判定が必要(E16)。** 自動検証で言えるのは「AIの既知経路で完走できる」ことだけで、体感の難易度・怖さ・初見の分かりやすさは判定していない。今回の変更は難易度に直接効く。
  - **敵は登れない(フェーズ5)。** 天面と壁の上は現状ほぼ安全。空腹と渇きが進むので居座りは損をするが、意図した緊張が緩む可能性がある。
  - **暗闇の扇形は床面の2Dレイキャストのまま(フェーズ4)。** 壁に貼り付いている間、扇形は体からは出るが遮蔽計算は床基準。指示書どおり選択肢 (a) を維持している。
  - 面の端まで来ると止まる(角を回り込んで隣の面へ移らない)。指示書 §2 の「法線を補間して乗り移る」は未実装。
  - 天井は今回も描いていない。俯瞰の妨げになるため。
  - **FPS は3フェーズ通して未計測。** headless は SwiftShader。障害物が7→15、床面積が約1.5倍、影の面積も増えたので、**Iris Xe の実機で計測してほしい**。
  - 既定のカメラ(俯角38°)だと壁に貼り付いた姿勢が縮んで見える。`?cam=45,25,16,7` のほうが分かりやすい。既定値はガクチョが承認した角度なので勝手に変えていない。
- **オーナー解放**: `src/main.ts` `src/simulation.ts` `src/molt.ts` `*.mjs` を本セッション中のみ保持し、終了時に解放。

### 2026-09-08 / Claude / 斜俯瞰リファクタ フェーズ2 — 世界を床面(X-Z)に組み直し

- **やったこと**: 指示書フェーズ2「世界のジオメトリを床面ベースに組み直す。この段階ではプレイヤーは床だけ歩ければよい」。ガクチョの指示により**世界の構造のみ**を対象とし、ゴキブリのビジュアル(Blender で別途制作中)には手を入れていない。
  1. `src/space.ts` を新設。sim(x,y) → world(x, 高さ, -y) の対応と、生物を床に寝かせる台座 `grounded()` を1箇所に集約。座標の読み替えが散らばらないようにした。
  2. **`src/simulation.ts` は1行も変更していない。** sim の `{x,y}` は指示書 §1 の言う「接触面上の2Dパラメータ空間」としてそのまま温存し、床の上の座標として読み替えるだけにした。→ 難易度・敵AI・資源配置は完全に不変(`playthrough.mjs` が変更前と同一の 218.1秒 / state=won を出すことで確認)。
  3. 世界の作り直し: 壁の断面 → 屋根裏の床。床板6枚+下地、手前の低い根太、**奥の壁(高さ3.2 — フェーズ3で登る面)**、通路両端の壁、奥壁の足元に断熱材、頭上に疎な梁(床に影の縞を落とす)、壁を登る配線と床際を走る配線。木くず190個と埃450粒も床基準に置き直した。
  4. `OBSTACLES` の7つの矩形を「床の footprint」として読み替え、高さを与えて立体の塊にした。高さ `OBST_H` は **AI暫定値**(プレイヤーが隠れないよう1.3以下)。材質は従来どおり木材。
  5. 生物は `grounded()` の台座で包み、床に寝かせた。`animateAnimal` が触る `rotation.z`(進行方向)がそのまま world Y 軸まわりの回頭になるため、**`src/animals.ts` は編集していない**。指示書の Quaternion 版に差し替わってもこの台座は残せる。
  6. 指向性ライトをプレイヤー追従にし、影のカメラを ±60 → ±18 に絞った(床面になって影の面積が増えたため、解像度と負荷の両方で必要)。
  7. カメラ: 俯角の意味が「壁を見上げる角」から「床を見下ろす角」に変わったので既定値を入れ直した(方位30° / 俯角38° / 距離12)。カメラを **-X 側(進行方向の後ろ)** に移し、注視点を 3.5 だけ前に置いて進む先が見えるようにした。
  8. 暗闇はスクリーン空間(指示書の選択肢 a)のまま。傾きに追随させるため、障害物の輪郭を矩形 → 四隅を結ぶ多角形に、危険方向の指示をワールド角 → 画面上の方向に変えた。
- **変更ファイル**: `src/space.ts`(新規) `src/main.ts` `src/molt.ts` `map.md` `artifacts/world-*.png`(新規8枚)
- **ゲーム本体の変更**: あり(描画のみ。**シミュレーションは不変**)
- **検証**: `bash scripts/verify.sh` **全項目 OK**(build / smoke 9群 / playthrough **218.1秒 state=won — フェーズ1時点と完全に同値** / browser-audit PASS)。加えて Playwright で 孵化 / 障害物脇 / クモ巡回域 / 糞の山 / 脱皮中 を撮影し、ページ例外0件。証跡は `artifacts/world-a-default.png`〜`world-h-molt.png`。
- **未完・引き継ぎ**:
  - **FPS は今回も未計測**(headless は SwiftShader)。床面化で描画面積と影の面積が増えているので、**Iris Xe の実機で計測してほしい**。重ければ削る順は 梁の影 → 指向性ライトの shadow.mapSize 2048→1024 → 床板の枚数。
  - **天井を描いていない。** 屋根裏なのに上が抜けている。俯瞰の妨げになるため意図的に省いた。フェーズ4(暗闇の作り直し)で、視界外だけに天井を出すなどの手当てが要る。
  - 通路は 100 × 7 のまま。指示書の言う「奥行き方向の広がり」は入れていない。**広げると移動距離が変わり、ガクチョが確定させた難易度に直接効く**ため、単独の判断として切り出す。
  - `simulation.ts` の変数名は `y` のまま(床を横切る方向)。フェーズ3で面ベースに拡張する際に改名する。今改名すると全行が差分になる。
  - 壁登り(フェーズ3)は未着手。奥の壁は立てただけで、登る判定はない。
  - `molt.ts` は台座に合わせて位置指定を書き換えた。脱皮中の縮みを「画面の縦」→「背丈(モデル局所Z)」に付け替えている。
- **オーナー解放**: `src/main.ts` `src/molt.ts` を本セッション中のみ保持し、終了時に解放。

### 2026-09-08 / Claude / 斜俯瞰リファクタ フェーズ1 — カメラのみ透視投影に変更

- **やったこと**: `docs/isometric-refactor-brief.md` の進め方に従い、**フェーズ1(カメラだけ傾ける)の範囲に限定**して実装。
  1. `OrthographicCamera` → `PerspectiveCamera(fov 45)`。方位角・俯角・距離を極座標オフセットで与え、注視点をプレイヤーに `lerp` 追従させる(従来と同じ減衰係数)。
  2. カメラ定数を先頭に集約: `CAM_FOV/CAM_YAW/CAM_PITCH/CAM_DIST/CAM_HEIGHT/CAM_LERP/CAM_CATCHUP/PLANE_Z`。**すべて AI暫定値(ガクチョ未指定)**。`?cam=fov,yaw,pitch,dist` で再ビルドなしに比較できる。既定は fov45 / 方位18° / 俯角26° / 距離14。
  3. 傾けたことで壊れる箇所を追随させた —
     - 照準: `unproject`(平行投影前提)→ レイと `z=PLANE_Z` 平面の交差。
     - `project()`: `z=.1` → `z=PLANE_Z`(生物と同じ平面。傾けると z の差が画面位置に出るため)。
     - `darkness()` の world→px 倍率: 定数 `innerHeight/11.5` → プレイヤー位置で実測(透視投影では一定でない)。
     - `resize()`: 平行投影の frustum → `camera.aspect`。
     - シミュレーション更新ゲートの `camera.right*.8` → `CAM_CATCHUP`(透視投影に `right` が無い)。
- **変更ファイル**: `src/main.ts` `map.md` `artifacts/cam-*.png`(新規4枚)
- **ゲーム本体の変更**: あり(`src/main.ts` のみ。`simulation.ts` / `animals.ts` / `OBSTACLES` は未変更)
- **検証**: `bash scripts/verify.sh` **全項目 OK**(build / smoke 9群 / playthrough 218.1秒 state=won / browser-audit PASS)。加えて Playwright で4通りのカメラ角(正面 / 既定18-26 / 30-35 / 指示書の45-40)を同一地点(x=20)で撮影し、ページ例外0件を確認。証跡は `artifacts/cam-a-frontal.png` `cam-b-default.png` `cam-c-30-35.png` `cam-d-brief-45-40.png`。
- **未完・引き継ぎ**:
  - **指示書の狙い(俯角35〜50° / 方位45°)を今の世界にそのまま当てると成立しない**ことをスクリーンショットで確認した。現在の世界は X-Y 平面に建てた壁の断面なので、大きく傾けるほど遊技面が画面内で斜めに寝て、指示書の言う「書き割り」になる(`cam-d-brief-45-40.png`)。**フェーズ2(世界を X-Z 床面に組み直す)を通さないと指示書の絵にはならない。** 既定値を 18°/26° に抑えたのはこのため。
  - 方位角を付けると通路(X軸)が画面上で斜めに走るため、WASD の入力方向と画面上の移動方向がずれる。フェーズ2で入力をカメラ基準に直す必要がある。
  - **FPS は未計測**。headless Chromium は SwiftShader(ソフトウェア描画)で約5fps しか出ず、Iris Xe の指標にならない。指示書の「フェーズごとにFPS計測」はガクチョの実機で行う必要がある。
  - `src/animals.ts` は指示書のとおり未編集。`animateAnimal` の `heading:number` → `Quaternion` 移行アダプタも**まだ置いていない**(フェーズ3で姿勢が必要になった時点で入れる)。
  - 暗闇は指示書の選択肢 **(a) スクリーン空間のまま**で通した。傾いた面に光は正しく乗らない(既知の割り切り)。
- **オーナー解放**: `src/main.ts` を本セッション中のみ保持し、終了時に解放。

### 2026-09-08 / ガクチョ / 実プレイ判定 — 難易度は現状維持で確定

- **判定**: 改修後の版を実プレイし、**「設計意図通りの難易度」**。難易度に関する調整要求はなし。
- **確定した設計**: 序盤の資源7個は維持用にとどめ、脱皮に必要な蓄えは x=76(餌4.0) / x=78(水4.0) を取りに行かせる。x=78 にクモ2匹目を置くことで餌場が章のクライマックスになる、という構成をそのまま維持する。資源9個・糞3箇所はすべて座標固定(乱数なし)。
- **HUDの初期表示について**: 開始時に餌3/5・水2/5 が埋まっている件は、序盤資源が微量なため「空から始めると印がほとんど動かず、集めても埋まらないという誤解を招く」と判断し、**現状維持**とした。
- **未取得**: 所要時間、死因、群れの減少曲線、選んだ脱皮場所。5〜8分の条件は引き続き未検証。
- **変更ファイル**: なし(コード変更なし)

### 2026-09-08 / Claude / ガクチョのプレイ後フィードバック4件を実装

- **やったこと**: 実プレイを受けた4点を実装。
  1. 仲間の死亡通知を「白い帯がひとつ、消えた。」→「Gが{n}体、減りました」に変更。同時に複数減った場合は実数を表示する(`lossCount`)。
  2. HUD に `#molt-req`「脱皮に必要な蓄え」を新設。餌・水を5段階の ●○ で表示し、しきい値 0.85 に対する充足を `Math.floor` で埋める(満たす前に満杯表示にならない)。満了時は「準備ができました — 安全な場所で Space を長押し（3秒）」、餌水が満ちても体力不足なら「体力が足りない — 糞の山で休む」。
  3. 全体を明るく。`VEIL_ALPHA` 等の調整用定数を `darkness()` の直前に集約し、1箇所で詰められるようにした。
  4. 全滅時のキャプションを「白い帯は、もう動かない。」→「Gの群れは、全滅した。」に変更。
- **変更ファイル**: `src/main.ts` `src/style.css` `artifacts/*.png`(検証画像) `map.md`
- **ゲーム本体の変更**: あり(`src/main.ts` / `src/style.css`)
- **検証**: `bash scripts/verify.sh` **全項目 OK**(build / smoke 9群 / playthrough 218.1秒 state=won / browser-audit PASS)。加えて Playwright で HUD の3状態(空・途中・準備完了)、減少通知の文言、全滅キャプション、ページ例外なしを実測。明るさは合成後スクリーンショットの中央域平均輝度で **5.03 → 7.47(約1.5倍)** を確認。証跡は `artifacts/hud-empty.png` `hud-ready.png` `ending-lost.png` `brightness-before.png` `brightness-after.png`。
- **未完・引き継ぎ**:
  - 明るさは「環境の輪郭が薄く見える」程度まで上げた。敵・仲間・餌は従来どおり `visible()` で遮蔽されるため、**静的な地形だけが見えるようになっている**。行き過ぎ/不足はガクチョの体感で `src/main.ts:66` の定数を調整する。
  - 開始時点で餌3/5・水2/5 が埋まった状態で始まる(初期値が0ではないため)。「空から埋まる」感を強めたいなら初期値を下げる必要があり、難易度に直結するため未実施。
  - C17 の視覚面(白帯が消えて闇に溶ける)は未着手のまま。
- **オーナー解放**: `src/main.ts` を本セッション中のみ保持し、終了時に解放。Codex は未稼働のため競合なし。

### 2026-09-08 / Claude / 共同運用の基盤づくり

- **やったこと**: リポジトリと仕様(`gokiburi_ch1_spec.md` / `SPEC_GAP_AUDIT.md` / `IMPLEMENTATION_STATUS.md` / `src/*.ts`)を読み込み。Claude-Codex 共同運用のための `map.md` / `CLAUDE.md` / `AGENTS.md` を新規作成。検証コマンドを `scripts/verify.sh` にまとめ、`.claude/settings.json` で頻出コマンドを許可。
- **変更ファイル**: `map.md`(新規) `CLAUDE.md`(新規) `AGENTS.md`(新規) `scripts/verify.sh`(新規) `.claude/settings.json`(新規)
- **ゲーム本体の変更**: なし(`src/` は未編集)
- **検証**: `bash scripts/verify.sh` を実行。`npm run build` OK / `smoke.mjs` OK / `playthrough.mjs --probe` OK(218.1秒、操作交代1回、最後は1匹、state=won)。`browser-audit.mjs` は当初 SKIP だったが、開発サーバー起動後に再実行して **PASS**(脱皮中の座標固定、脱皮中のアリ3秒捕食、残数HUD、人間イベント減衰、音声Context、ページ例外なし)。
- **未完・引き継ぎ**: ガクチョの方針は「まず実プレイ検証(E12)」に決定。開発サーバーを起動し、ガクチョの通しプレイ待ち。プレイ後の指示で P1 の着手対象を決める。
  - `git push origin main` 完了(`cd49511..366f11d`)。Codex は origin/main から map.md / AGENTS.md を読める状態。
- **ガクチョ判断待ち**: E12 実プレイの記録項目 = 所要時間 / 死因 / 群れの減少曲線 / 選んだ脱皮場所 / どこで迷ったか。
- **オーナー解放**: なし(取得していない)

---

## 7. セッションログ テンプレート

```markdown
### YYYY-MM-DD / (Claude|Codex) / 一行見出し

- **やったこと**:
- **変更ファイル**:
- **ゲーム本体の変更**:
- **検証**: (実行したコマンドと結果。未実行なら「なし」と書く)
- **未完・引き継ぎ**:
- **オーナー解放**: (§2 で取得した領域を返したか)
```
