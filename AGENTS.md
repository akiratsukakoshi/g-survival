# AGENTS.md — このプロジェクトでの Codex の動き方

G-survival(第1章)を、**ガクチョ・Codex(ChatGPT)・Claude Code の3者**で進める。
Claude 向けの同内容は `CLAUDE.md` にある。共有台帳は `map.md`。
**両ファイルは同じ運用ルールを指す。片方だけ更新しないこと。**

---

## 0. 大前提

- 応答は日本語。ユーザーの呼称は **「ガクチョ」**。
- 一次仕様は `gokiburi_ch1_spec.md`。**このファイルは変更しない。**
  仕様から意図的に外れる判断は `map.md` §4「仕様変更ログ」に記録する。
- `SPEC_GAP_AUDIT.md` は 2026-09-06 時点のスナップショット。**現在の未実装一覧として引用しない。**
- 原仕様 §9「フェアネス規約」は設計の憲法として扱う。
  1. 予告のない即死を作らない(0.4秒以上の兆候を必ず置く)
  2. 敵の行動に乱数を使わない
  3. 上達が安全に直結する
  4. ジャンプスケアを使わない
  5. 説明テキストで教えない

---

## 1. セッション開始時(必須)

1. `map.md` を読む。
2. §3 INBOX の `OPEN` を確認する。自分宛(`→Codex`)があれば、作業に入る前に処理する。
3. §2 のオーナーシップを確認する。**Claude が「作業中」の領域には触らない。**
4. `git pull --rebase origin main` で最新化する。

## 2. セッション終了時(必須)

1. `map.md` §6 の**先頭**にセッションログを追記する(テンプレは §7)。
   - 「やったこと」より **「検証したこと」と「未完」** を正確に書く。
2. §2 で取ったオーナーを外す。
3. §4 バックログの状態を更新する。
4. Claude に伝えるべきことがあれば §3 INBOX に `Codex→Claude` で追加する。
5. コミットする(§4 の規約)。

## 3. Claude との連絡

- 連絡手段は `map.md` §3 INBOX のみ。
- 依頼するときは 何を・なぜ・完了条件 を書く。
- 相手のコードを否定せず、事実(再現手順、該当行、仕様のどの項に反するか)を書く。
- 相手のコミットを revert / force-push しない。仕様違反に見えても INBOX で指摘し、ガクチョの判断を仰ぐ。

## 4. Git

- `main` 直コミット。作業前に必ず `git pull --rebase origin main`。
- メッセージ冒頭に `[codex]` を付ける。1コミット1論点。
- push はガクチョの指示があるときだけ。

## 5. コードの書き方

- 既存の高密度スタイルを維持する。整形・リファクタはガクチョの承認を得てから、単独のコミットで。
- `src/main.ts` は1行が最長1492文字ある。広い範囲の書き換えを1コミットに混ぜない。
- 着手前に `map.md` §2 でオーナーを取り、終わったら外す。
- 新しい調整値(探知距離、予告秒数、匹数)は、**AIの暫定値かガクチョの指定値か**を区別して記録する。

## 6. 検証

```bash
npm run build
bash scripts/verify.sh
```

自動検証で判定できないもの(恐怖感、初見の分かりやすさ、実プレイ所要時間、実スピーカー音量)は、
**判定済みとして報告しない。** `map.md` にガクチョの実プレイ判断待ちとして残す。

## 7. やらないこと

- `gokiburi_ch1_spec.md` の編集
- 承認なしの大規模リファクタ
- Claude が作業中の領域への書き込み
- 未検証の内容を「動作確認済み」と書く
- 難易度の数値だけを動かして、未実装のコアシステムの代わりにする

## 8. 共同引き継ぎの補足（両エージェント共通）

- 正式な運用ファイルはCodex用 `AGENTS.md` とClaude用 `CLAUDE.md` のみ。別名の運用ファイルは作らない。共通ルールを変更する場合は両正式ファイルを同じコミットで同期する。
- 開始時に `git status --short` と直近ログも確認する。未コミット変更を他者の作業か検証生成物か区別し、勝手に消さない。pullに支障があれば対象を限定して退避し、pull後に復元して競合を確認する。
- pull後に `map.md` の更新・オーナー・自分宛INBOXを再確認する。相手宛OPENを相手の代わりに完了扱いにしない。依頼が自分の今回の作業で置き換わる場合は、その事実を追記する。
- まとまった作業の区切りごとに、終了の合図がなくても §6 先頭へ記録する。変更ファイル、実行した検証と結果、未検証項目、残作業、オーナー解放、commit/push状態を残す。push済みの過去記録は新しいpush許可ではない。
- INBOXの完了連絡には、対象ファイル、変更されたAPI/座標/素材規約、再現・検証方法、次の担当が必要な作業と完了条件を記載する。チャットだけで申し送りを完結させない。
- 生物のモデル座標は現行APIで +X前方/+Z背中。接触面の姿勢は `space.ts` の台座が担当する。glTFの -Z前方/+Y上は読み込み層で変換し、simulationの移動・判定と混同しない。
- Blender編集元は `C:\Users\tukap\blender-work`、配布GLBは `public/models/`、再生成元は `assets/nymph-source/`。スキン付き個体は骨を共有せず複製し、材質の個体差・脱皮・リセットも検証する。
- `helper_unknown_error: setup refresh had errors` が出た場合は、Linux権限変更や設定変更を試さない。原因はWindowsサンドボックスが `\\wsl.localhost` にACLを付ける前処理である。以後のコマンドはWindows側の安定した作業ディレクトリから、承認済みの `wsl -e bash -ic 'cd /home/tukapontas/g-survival && ...'` で実行する。編集はWindows側の書き込み可能な一時領域でパッチ／変換スクリプトを `apply_patch` で作成し、WSL内部で `git diff --check` と内容確認後に対象ファイルへ反映する。UNCを作業ディレクトリにした通常実行や、同じ失敗経路の反復をしない。
- WSL内のNode/npmを使用する。Windows npmが混ざる場合は利用可能な `~/.nvm/versions/node/` を確認し、そのbinをPATH先頭へ置く（回帰検証はNode 23以降）。Windows側から起動する場合はパスの空白に注意する。
- 必須検証はbuildと `scripts/verify.sh`。モデル変更時は読み込み失敗、複数個体の独立性、進行方向と壁姿勢、停止/歩行、脱皮の白化/復色と再開始を追加検証する。ブラウザーSKIPは合格に数えない。実機FPSや見た目の最終判断はガクチョ待ちとして分ける。