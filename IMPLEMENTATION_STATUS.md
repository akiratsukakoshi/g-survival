# 第1章 修正・検証記録

更新: 2026-09-07。初期仕様は `gokiburi_ch1_spec.md`、修正前の監査は `SPEC_GAP_AUDIT.md` を参照。監査文書を過去の状態として保存し、現在の結果をここに分けて記録する。

## 今回反映した修正

| 項目 | 実装・確認内容 |
|---|---|
| 脱皮 | Spaceを3秒保持して開始。20秒移動不可。背中の裂け目、白い体の抽出、脚と触角の引き抜き、硬化、1.3倍の成長と抜け殻。死亡・個体交代で進捗をリセット |
| アリ | 接触した個体を追い、周囲のアリも列から離れて群がる。連続接触3秒で死亡。プレイヤー・仲間・脱皮中に適用。逃走して離れると接触蓄積が減る |
| 行列 | 障害物の外を周回。餌を持つアリは餌場を通る。追跡中は巡回・瞬間移動をしない |
| クモ | 固定巡回、視野と遮蔽物の判定、ダッシュ振動の検出。0.8秒予告で方向を固定し、跳躍1回の捕食は1匹まで。捕食後または跳躍後に回復時間 |
| 発見されやすさ | 壁際の速度+30%。視覚の検出距離係数は壁際0.5、開放部3。静止時0.12、脱皮中5。ダッシュ振動は別に検出 |
| 群れ | 操作中を含め24匹。個別の経路で散開、障害物を迂回して移動。敵による自律的な捕食あり。死亡時に生存個体を疑似乱数で選び、1秒の保護とカメラ追従 |
| 糞 | 3箇所を造形・探知表示。周辺で仲間が集合、栄養・体力・スタミナ回復。水の無限回復は廃止 |
| 枯渇 | 水・栄養が尽きると体力が減る。水のほうが速く弱る。探知範囲と移動速度にも影響。事前のHUD警告 |
| 地形 | 床・天井から交互に張り出す7障害物。上端直進の抜け道を閉じ、迂回導線に変更。触角の遮蔽と地形の記憶輪郭 |
| 補給 | 序盤は維持用の少量、終盤はまとまった餌と水。時間比例で摂取し、満腹時に資源を浪費しない。序盤の補給だけでは脱皮条件に届かない |
| 人間 | x40・x75でそれぞれ一度だけ。足音、上方の気流、揺れ、埃。ダメージなし |
| 音・表示 | BGM、音量・ミュート、予告開始からの警告音、残数・目的・状態、孵化演出。アリの向きと餌運びを実際の行動に同期 |
| 描画負荷 | 静的な背景と生物パーツを材質ごとに結合し、描画呼び出しを削減 |

## 自動検証

- `npm run build`：TypeScript検査と本番ビルド成功。three.jsを含むバンドルの500KB超注意表示あり（エラーなし）。
- `node --experimental-strip-types smoke.mjs`：9グループ成功。8方向・壁・遮蔽、脱皮の実時間、アリの3秒捕食と離脱、仲間の捕食、クモの予告と単発捕食、枯渇、序盤の補給制限、人間イベントの一度性を確認。
- `node --experimental-strip-types playthrough.mjs --probe`：敵・消耗・衝突を無効化せず、全補給地点から奥の隙間へ到達、脱皮完了。218.1秒、操作交代1回、最後は1匹。既知の地形経路を使い、警告の方向に応じて回避、アリ付近でダッシュする自動操作。初見の人間プレイの測定ではない。
- 通し検証の格子探索で、6区画と各補給地点・終点への通路を確認。
- `node browser-audit.mjs`：成功。実Space入力、脱皮中の座標固定、脱皮中のアリ3秒捕食と残数HUD、人間イベントの減衰、音声Context有効、ページ例外なし。`artifacts/molt-middle.png` を目視確認。`artifacts/ants-after-molt-catch.png` も保存。

## 初期仕様からの変更と評価上の限界

three.js/TypeScript、導入文、HUD、BGM、独立した群れ、ランダムな操作交代は追加要望を優先した変更。当初はAI暫定値として12匹だったが、2026-09-08のガクチョ指定で卵鞘の兄弟数に合わせ24匹へ戻した。リアル寄りの立体造形で、原仕様の敵の白い線画のみの表現とは異なる。

想定5〜8分は、初見での探索・迷い・待機を含めた実プレイではまだ確認できていない。既知経路の自動操作は約3分38秒なので、初期MDの所要時間条件を検証済みとは扱わない。恐怖感、無説明での学習、実スピーカーでの音量も自動検証だけでは判定しない。現状の検証値を残し、完成条件との混同を避ける。

`audit-probes.mjs` は修正前の問題を調べた診断用で、現行版の合否判定には使わない。ブラウザの `?test=1` は開発時の検証用フックで、本番ビルドには含まれない。
