# CLAUDE.md — このプロジェクトでの Claude の動き方

G-survival(第1章)を、**ガクチョ・Claude Code・Codex(ChatGPT) の3者**で進める。
Codex 向けの同内容は `AGENTS.md` にある。共有台帳は `map.md`。

---

## 0. 大前提

- 応答は日本語。ユーザーの呼称は **「ガクチョ」**。
- このゲームの一次仕様は `gokiburi_ch1_spec.md`。**このファイルは変更しない。**
  仕様から意図的に外れる判断は `map.md` §4「仕様変更ログ」に記録する。
- `SPEC_GAP_AUDIT.md` は 2026-09-06 時点のスナップショット。**現在の未実装一覧として引用しない。**
- 原仕様 §9「フェアネス規約」は設計の憲法として扱う。これに反する実装は提案しない。
  1. 予告のない即死を作らない(0.4秒以上の兆候を必ず置く)
  2. 敵の行動に乱数を使わない
  3. 上達が安全に直結する
  4. ジャンプスケアを使わない
  5. 説明テキストで教えない

---

## 1. セッション開始時(必須)

1. `map.md` を読む。
2. §3 INBOX の `OPEN` を確認する。自分宛(`→Claude`)があれば、作業に入る前に処理するか、処理しない理由をガクチョに伝える。
3. §2 のオーナーシップを確認する。**Codex が「作業中」の領域には触らない。**
4. `git pull --rebase origin main` で最新化する。

## 2. セッション終了時(必須)

1. `map.md` §6 の**先頭**にセッションログを追記する(テンプレは §7)。
   - 「やったこと」より **「検証したこと」と「未完」** を正確に書く。検証していないものを検証済みと書かない。
2. §2 で取ったオーナーを外す。
3. §4 バックログの状態を更新する(着手/完了/新規発見)。
4. Codex に伝えるべきことがあれば §3 INBOX に `Claude→Codex` で追加する。
5. コミットする(§4 の規約)。

> ガクチョから「終わり」「今日はここまで」等の合図がなくても、**まとまった作業を終えたらログを書く**。途中でセッションが切れても引き継げる状態を保つ。

## 3. Codex との連絡

- 連絡手段は `map.md` §3 INBOX のみ。口頭伝達や記憶に頼らない。
- 相手に**依頼**するとき: 何を・なぜ・完了条件を書く。「よろしく」で終わらせない。
- 相手のコードを**否定しない**。事実(再現手順、該当行、仕様のどの項に反するか)を書く。
- 相手の判断が仕様違反に見えても、勝手に巻き戻さない。INBOX で指摘し、ガクチョの判断を仰ぐ。
- Codex のコミットを revert / force-push しない。

## 4. Git

- `main` 直コミット。作業前に必ず `git pull --rebase origin main`。
- メッセージ冒頭に `[claude]` を付ける。1コミット1論点。
- push はガクチョの指示があるときだけ行う。コミットは作業の区切りで行ってよい。
- コミット末尾に付ける:
  `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`

## 5. コードの書き方

- **既存のスタイルに合わせる。** このコードベースは意図的に高密度(1行に複数文、短い変数名)。
  勝手にフォーマットを変えない。整形・リファクタは**ガクチョの承認を得てから**、単独のコミットで行う。
- `src/main.ts` は1行が最長1492文字ある。**差分が読めなくなるため、広い範囲の書き換えを1コミットに混ぜない。**
- 着手前に `map.md` §2 でオーナーを取る。終わったら外す。
- 新しい定数(探知距離、予告秒数、匹数など)は、**AIの調整値なのかガクチョの指定値なのか**を区別して記録する。
  過去に「12匹」がAI暫定値のまま既定になった経緯がある。

## 6. 検証

```bash
npm run dev                  # 開発サーバー(WSL、--host 0.0.0.0)
npm run build                # tsc --noEmit + vite build
bash scripts/verify.sh       # 下の一括実行
```

`scripts/verify.sh` の内訳:
- `npm run build`
- `node --experimental-strip-types smoke.mjs`(Node 23以降が必要。現在 v23.3.0)
- `node --experimental-strip-types playthrough.mjs --probe`
- `node browser-audit.mjs`(**開発サーバー起動中のみ**。Playwright の Chromium が必要)

### 検証の限界を偽らない

自動検証で**判定できないもの**は、判定済みとして報告しない。
- 恐怖感、初見での分かりやすさ、無説明での学習
- 実プレイの所要時間(自動操作の約3分38秒は既知経路の走行であり、想定5〜8分の検証ではない)
- 実スピーカーでの音量バランス

これらは「ガクチョが実際に遊んで判断する」項目として `map.md` に残す。

## 7. やらないこと

- `gokiburi_ch1_spec.md` の編集
- ガクチョの承認なしの大規模リファクタ・フォーマット変更
- Codex が作業中の領域への書き込み
- 検証していないことを「動作確認済み」と書く
- 難易度の数値だけを動かして、未実装のコアシステムの代わりにする
